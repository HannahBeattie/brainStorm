import {
	Box,
	Heading,
	HStack,
	Icon,
	Image,
	Spacer,
	Text,
	useColorModeValue,
	VStack,
} from '@chakra-ui/react'
import Head from 'next/head'
import { SiCircle } from 'react-icons/si'
import { GiPlantsAndAnimals } from 'react-icons/gi'
import MyLink from '~/components/MyLink'
import { GiFireRing } from 'react-icons/gi'
import Nav from '~/components/nav/Nav'
import { motion } from 'framer-motion'
export default function Home() {
	const vProps = {
		w: '100vw',
		flex: '1',
	}

	return (
		<VStack {...vProps} spacing={'0'} alignItems={'stretch'}>
			<Head>
				<title>Create Next App</title>
				<meta name='description' content='Generated by create next app' />
				<link rel='icon' href='/favicon.ico' />
			</Head>
			<HStack justify={'center'} my={'8'} color={'gray.400'} _dark={{ color: 'gray.500' }}>
				<Heading textAlign={'center'} fontSize={'9xl'} fontWeight={'900'}>
					Hell
				</Heading>
				<Icon
					fontSize={'8xl'}
					mt={2}
					as={useColorModeValue(GiPlantsAndAnimals, GiFireRing)}
				/>{' '}
				<Heading textAlign={'center'} fontSize={'9xl'} fontWeight={'900'}>
					W
				</Heading>
				<Icon fontSize={'8xl'} as={useColorModeValue(GiPlantsAndAnimals, GiFireRing)} />
				<Heading textAlign={'center'} fontSize={'9xl'} fontWeight={'900'}>
					rld
				</Heading>
			</HStack>

			<VStack position={'relative'} spacing={'0'}>
				<Box position={'absolute'}>
					<Image
						src={useColorModeValue('bg.jpg', '/godCreatesDev.jpg')}
						zIndex={'0'}
						h={'400'}
						w={'100vw'}
					/>
				</Box>
				<motion.div
					animate={{ rotate: [0, 2, -2, 0] }}
					transition={{ duration: '14', repeat: Infinity, ease: 'easeInOut' }}
					whileTap={{ rotate: [3, 4, -4, 3], scale: [1, 1.1, 1] }}
				>
					<Box position={'relative'}>
						<Image mt={'-90'} src={'/char.png'} />
					</Box>
				</motion.div>
			</VStack>
		</VStack>
	)
}
